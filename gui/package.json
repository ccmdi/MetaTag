{
  "name": "meta-tag",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.js",
  "scripts": {
    "start": "set NODE_ENV=development&&electron .",
    "build": "electron-packager . MetaTag --platform=win32 --arch=x64 --out=dist --asar --overwrite --ignore=\"(node_modules|dist|build)\" --icon=../MetaTag.ico"
  },
  "build": {
    "extraResources": [
      {
        "from": "extraResources/",
        "to": "extraResources",
        "filter": ["**/*"]
      }
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^31.1.0",
    "electron-packager": "^17.1.1"
  }
}